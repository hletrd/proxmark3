# proxmark3

Research workspace for **Proxmark3** (and derivatives) and **MIFARE** tags.

Upstream firmware/client sources are vendored here as **git subtrees** so they can be
updated with `git subtree pull` while keeping full local buildability.

> Detailed device notes, build/flash instructions, and research docs are being added.
